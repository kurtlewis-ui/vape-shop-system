# Base URL of the backend API. The frontend talks to the NestJS backend here.
# Copy this file to ".env.local" to customize. If omitted, the app defaults to
# http://localhost:4000/api/v1
NEXT_PUBLIC_API_URL=http://localhost:4000/api/v1
